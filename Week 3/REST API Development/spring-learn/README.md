# Spring Boot Fundamentals Execution

A concise micro-app architecture demonstrating a litany of underlying Spring Boot functionality securely scaling classical `<bean>` definitions into highly organized REST-like execution scopes gracefully.

## Highlights
* **Project Genesis:** Initialized automated development capabilities structuring DevTools and automated Web mechanisms natively.
* **Property Scoping Execution:** Bridged sophisticated configuration parameters loading pre-defined `SimpleDateFormat` logic natively overriding explicit string parsing natively using standard `ClassPathXmlApplicationContext`.
* **Logging Protocols:** Enforced deep property boundaries customizing format tracking strings uniformly configuring global `.info()` logs executing across `.debug()` traces utilizing explicit `logging.pattern.console` within `application.properties`.
* **Scoping Architectures:** Demonstrated sophisticated dependency controls dynamically mapping shared singletons properly up against highly customized `prototype` tracking mechanics enforcing object recreation natively nested safely outside `ArrayList` injection contexts directly.
